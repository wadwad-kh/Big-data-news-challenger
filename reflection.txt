If the input grew 1000×, the step that would break first is the streaming aggregation state, especially the keyword counting and hourly window aggregation.
Spark would need to keep much more state in memory, which can slow the pipeline or cause memory problems.
To fix this, I would use Spark watermarks to remove old state, checkpointing for recovery, and eventually run the job on a real cluster instead of local mode.